# Vibe-coded by Antigravity v2.0 on 2026-08-13. Driven by @dt1900.
name = "pocket-notion-sync"
main = "src/adapters/cloudflare.ts"
compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat"]

# Environment variables (set secrets via `wrangler secret put NOTION_API_KEY`, etc.)
[vars]
# Add non-sensitive variables here if needed
